/// <reference types="react" />
import { IGOCardInfoProps } from "../types";
declare const IGODetailCard: ({ name, status, site, tokenImg, balance, address, networksTab, currentNetwork, handleTab, time, texts, linkView, externalLink, withTimer, width, }: IGOCardInfoProps) => JSX.Element;
export default IGODetailCard;
