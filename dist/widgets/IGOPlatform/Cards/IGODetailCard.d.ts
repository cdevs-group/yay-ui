/// <reference types="react" />
import { IGOCardInfoProps } from "../types";
declare const IGODetailCard: ({ name, status, site, tokenImg, balance, address, networksTab, currentNetwork, handleTab, time, texts, handleView, externalLink, withTimer, }: IGOCardInfoProps) => JSX.Element;
export default IGODetailCard;
