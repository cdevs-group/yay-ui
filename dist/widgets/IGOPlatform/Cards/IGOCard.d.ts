/// <reference types="react" />
import { IGOCardProps } from "../types";
declare const IGOCard: ({ status, token, tokenImg, amount, networksTab, handleTab, currentNetwork, dataSlots, statusText, currentVolume, totalVolume, texts, time, handleView, }: IGOCardProps) => JSX.Element;
export default IGOCard;
