/// <reference types="react" />
import { IGOCardProps } from "../types";
declare const IGOCard: ({ status, token, tokenImg, amount, networksTab, handleTab, currentNetwork, dataSlots, statusText, statusTextVisible, currentVolume, totalVolume, texts, }: IGOCardProps) => JSX.Element;
export default IGOCard;
