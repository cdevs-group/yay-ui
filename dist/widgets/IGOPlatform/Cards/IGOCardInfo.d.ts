/// <reference types="react" />
import { IGOCardInfoProps } from "../types";
declare const IGOCardInfo: ({ name, status, site, tokenImg, balance, address, networksTab, currentNetwork, handleTab, time, texts, handleView, externalLink, }: IGOCardInfoProps) => JSX.Element;
export default IGOCardInfo;
