import React from "react";
interface HeaderBridgeStakingProps {
    children: React.ReactNode;
    title: string;
    margin?: string;
    maxWidth?: string;
}
declare const HeaderBridgeStaking: ({ margin, title, children, maxWidth }: HeaderBridgeStakingProps) => JSX.Element;
export default HeaderBridgeStaking;
