/// <reference types="react" />
import { LobbyListItemProps } from "../types";
declare const LobbyListItem: ({ epoch, creator, bet, startTime, claimed, status, texts, handleButton, history, yourScore, apponentScore, winner, isLoad, lose, }: LobbyListItemProps) => JSX.Element;
export default LobbyListItem;
