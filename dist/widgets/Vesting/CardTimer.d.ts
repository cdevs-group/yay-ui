/// <reference types="react" />
interface IProps {
    data?: {
        text: string;
        value: string;
    };
}
declare const CardTimer: ({ data }: IProps) => JSX.Element;
export default CardTimer;
