/// <reference types="react" />
import { IGOCardV2Props } from "../types";
declare const IGOCardV2: ({ chance, status, handleStake, texts, handleView, token, statusTitle, tokenImg, amount, networksTab, currentNetwork, handleTab, tierStatus, time, isStaked, allocation, inPool, isClaimed, tperiodText, }: IGOCardV2Props) => JSX.Element;
export default IGOCardV2;
