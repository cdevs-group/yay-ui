/// <reference types="react" />
declare const InputFile: ({ margin, title, placeholder, onChange, name, }: {
    onChange: any;
    name: string;
    margin?: string | undefined;
    title: string;
    placeholder: string;
}) => JSX.Element;
export default InputFile;
