/// <reference types="react" />
declare const WinButton: ({ children, image, onClick }: {
    children: string;
    onClick: () => void;
    image: string;
}) => JSX.Element;
export default WinButton;
