/// <reference types="react" />
import { TabsProp } from "./types";
declare const Tabs: ({ tabValue, onClick, fontSize, tabsList, width, paddingTabs, colorActive, variant }: TabsProp) => JSX.Element;
export default Tabs;
