export interface CardBalanceProps{
  right?:boolean,
  title:string,
  text:string,
  children:any
};

export interface CardProductProp {
  title: string
  img: string
  bg: string
  closed?:boolean
}
